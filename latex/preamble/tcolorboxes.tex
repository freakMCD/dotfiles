\usepackage{needspace}

\AddToHook{env/proof/before}{%
    \Needspace{5\baselineskip}%
}

\usepackage{tcolorbox}
\tcbuselibrary{
    breakable,
    skins,
    theorems
}
\definecolor{LemaDark}{HTML}{1B3B2E}
\definecolor{LemaBack}{HTML}{F7FAF9}

\definecolor{AlgorithmDark}{HTML}{6B1E1E}
\definecolor{AlgorithmBack}{HTML}{FCF8F8}

\newtcbtheorem{lemabox}{Lema}{
    enhanced,
    breakable,
    colback=LemaBack,
    colframe=LemaDark,
    fonttitle=\bfseries\small,
    fontupper=\small,
}{lem}

\newtcolorbox{problem}[1]{%
    before upper={%
        \phantomsection
        \addcontentsline{toc}{section}{#1}%
    },
    enhanced,
    breakable,
    colback=white,
    colbacktitle=white,
    coltitle=black,
    borderline west={2.8pt}{0pt}{black!80},
    boxrule=0.25pt,
    title={#1},
    fonttitle=\bfseries,
    left=3mm,
    right=3mm,
    top=2mm,
    bottom=2mm,
    before skip=10pt,
    after skip=10pt
}
\newtcolorbox{algoritmo}[1][]{%
    enhanced,
    breakable,
    boxrule = 0pt,
    borderline west = {4pt}{0pt}{AlgorithmDark},
    colback = AlgorithmBack,
    coltitle = white,
    sharp corners,
    rounded corners = southeast,
    rounded corners = northeast,
    arc is angular,
    arc = 3mm,
    attach boxed title to top left,
    boxed title style = {%
        enhanced,
        colback = AlgorithmDark,
        colframe = AlgorithmDark,
        top = 0pt,
        bottom = 0pt,
        sharp corners,
        rounded corners = northeast,
        arc is angular,
        arc = 2mm,
        rightrule = 0pt,
        bottomrule = 0pt,
        toprule = 0pt,
    },
    overlay first = {%
        \path[fill=tcbcolback!80!black]
            ([yshift=3mm]interior.south east)
            -- ++(-0.4,-0.1)
            -- ++(0.1,-0.2)
            -- cycle;
    },
    overlay middle = {%
        \path[fill = tcbcolback!80!black]
            ([yshift = -3mm]interior.north east) -- ++(-0.4,0.1) -- ++(0.1,0.2);
        \path[fill = tcbcolback!80!black]
            ([yshift = 3mm]interior.south east) -- ++(-0.4,-0.1) -- ++(0.1,-0.2);
    },
    overlay last = {%
        \path[fill = tcbcolback!80!black]
            ([yshift = -3mm]interior.north east) -- ++(-0.4,0.1) -- ++(0.1,0.2);
    }
}
